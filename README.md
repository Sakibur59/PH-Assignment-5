1. const -it is a JavaScript keyword used to declare a variable whose value     cannot  be reassigned after it is defined.Also it's block scoped variable
    let - block scope variable. it's can be reassign. but we can not redeclare.
    var- it's function scope variable.  it's can be reassign also redeclare.

2. spread operator used for rest of array element. used for expand array element .

3. map - it is used when we want to change or transform each element of an array.
   filter- gives a new array based on condition.
   forEach- it's a loop. used in array's every element.

4. An arrow function is a shorter way to write functions in JavaScript (ES6).

5. inside a string allows embedding variables via backticks(``).
     For Example: `My name is ${name}`
